# Internet Deployment Configuration
DEPLOYMENT_MODE=internet
C2_HOST=0.0.0.0
C2_PORT=8443
C2_URL=
CUSTOM_DOMAIN=

# Authentication (auto-generated during deployment)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
JWT_SECRET_KEY=
JWT_EXPIRY_HOURS=24

# Agent Authentication
AGENT_API_KEY=
ENABLE_IP_WHITELIST=false
WHITELISTED_IPS=

# Database
DB_PATH=./c2.db

# SSL (handled by cloud platform)
SSL_ENABLED=false
